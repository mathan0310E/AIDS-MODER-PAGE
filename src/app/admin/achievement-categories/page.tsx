import { makeStringListPage } from "@/components/admin/StringListPage";

export default makeStringListPage({
  collection: "achievementCategories",
  title: "Achievement Categories",
  description: "Categories of student & faculty achievements.",
});
