import { makeStringListPage } from "@/components/admin/StringListPage";

export default makeStringListPage({
  collection: "downloadCategories",
  title: "Download Categories",
  description: "Document categories used to filter the downloads section.",
});
