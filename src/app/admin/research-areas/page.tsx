import { makeStringListPage } from "@/components/admin/StringListPage";

export default makeStringListPage({
  collection: "researchAreas",
  title: "Research Areas",
  description: "The domain tags shown in the research/curriculum area of the site.",
});
