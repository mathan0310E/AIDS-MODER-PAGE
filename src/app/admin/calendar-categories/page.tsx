import { makeStringListPage } from "@/components/admin/StringListPage";

export default makeStringListPage({
  collection: "calendarCategories",
  title: "Event Categories",
  description: "Colour-coded categories shown in the events calendar legend.",
});
